# Correção de inconsistências

Substitua os arquivos deste pacote na raiz do projeto.

Correção aplicada:
- Removidas do js/script.js as funções decorativas não chamadas:
  - initScrollProgress()
  - initHeroParticles()
  - initKickerBlink()
- Removidas chamadas residuais, caso existissem.
- Mantidas apenas as interações realmente executadas no site:
  - initRevealObserver()
  - initHeaderState()
  - initMobileMenu()
  - initContactForm()
  - initProductPage()
  - initProjectVideos()
  - initAccessibleMedia()
- HTMLs atualizados para carregar CSS/JS com cache ?v=20260703-6

Decisão:
Como a etapa anterior simplificou o visual e reduziu animações, não fazia sentido ativar partículas, blink e barra decorativa. A correção mais coerente foi remover código morto.

Commit sugerido:
git commit -m "refactor: remove funções JavaScript não utilizadas"
